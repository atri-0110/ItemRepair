rootProject.name = "ItemRepair"

pluginManagement {
    repositories {
        maven("https://maven.allaymc.org/releases")
        maven("https://maven.allaymc.org/snapshots")
        gradlePluginPortal()
    }
}
